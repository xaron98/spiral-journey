#!/bin/sh
# Xcode Cloud — post-clone script
# Runs after the repo is cloned on the build server.
# Add any setup steps here (e.g. installing tools, decrypting secrets).

set -e

echo "ci_post_clone: nothing to do"
