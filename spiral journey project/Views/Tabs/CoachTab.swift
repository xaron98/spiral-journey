import SwiftUI
import SpiralKit

/// Thin wrapper that hosts the new CoachHomeView. The previous honeycomb
/// implementation has been replaced by the bento + editorial feed design.
struct CoachTab: View {
    var body: some View {
        CoachHomeView()
    }
}
